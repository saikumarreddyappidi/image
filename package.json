{
  "name": "mern-image-search",
  "version": "1.0.0",
  "description": "Full-stack MERN application with OAuth authentication and Unsplash image search",
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "keywords": ["mern", "oauth", "unsplash", "mongodb", "express", "react", "nodejs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
